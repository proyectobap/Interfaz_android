<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyecto1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ClientMethods.Informacion"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BAP TICKETING"
        android:roundIcon="@mipmap/iconoapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".New_user"></activity>
        <activity android:name=".lista.Modificar_Evento" />
        <activity android:name=".lista.Evento" />
        <activity android:name=".lista.Tecnicos" />
        <activity android:name=".lista.Modificar_ticket" />
        <activity android:name=".lista.NewTicketAlone" />
        <activity
            android:name=".adds.Contenedor_Adds"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Loading" />
        <activity
            android:name=".Contenedor_tickets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Contenedor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Usuarios.User" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>